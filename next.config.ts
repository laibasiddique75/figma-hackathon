import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */

  
};


module.exports = {
  images: {
    domains: ['cdn.sanity.io'], // Sanity image domain add karein
  },
};


export default nextConfig;
